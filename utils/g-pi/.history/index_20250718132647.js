import { readdir, stat, readFile } from 'fs/promises';
import { join, extname } from 'path';

function processFile(path) {
  const name = "";
  const content = "";
}

function main() {
  // init
  // get analisis
  // display analisis
}

main();